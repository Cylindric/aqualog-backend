## 1. Temperature as a measurement parameter

- [x] 1.1 In `src/aquarium_measurements.py`, add `TEMPERATURE_PARAMETER = "temperature"` to `SUPPORTED_PARAMETERS`, and add `SUPPORTED_TEMPERATURE_UNITS = {"celsius", "fahrenheit"}`.
- [x] 1.2 Add `MIN_TEMPERATURE_CELSIUS`/`MAX_TEMPERATURE_CELSIUS` sanity-range constants and a `fahrenheit_to_celsius` conversion function.
- [x] 1.3 Extend `_validate_measurement_payload` and `_canonicalize_measurement` with a `temperature` branch (unit check against `SUPPORTED_TEMPERATURE_UNITS`, sanity-range check, conversion to canonical `celsius`), following the existing `salinity` dual-unit branch shape.
- [x] 1.4 Update the `_normalize_parameter` rejection message to include `temperature` alongside `salinity`/`phosphate`.
- [x] 1.5 Add temperature test cases to `tests/test_aquariums_measurements.py` (or the existing measurement test module): record/retrieve, unit conversion (`fahrenheit`→`celsius`), out-of-range rejection, missing-field rejection, duplicate-timestamp rejection, history ordering/filtering, case-insensitive parameter path.

## 2. Data model & migration (thresholds)

- [x] 2.1 Add `AquariumParameterThreshold` model to `src/models.py`: `id`, `aquarium_id` (FK → `aquariums.id`, `ondelete=CASCADE`, indexed), `parameter` (indexed), `target`/`min`/`max` (nullable `Float`), `unit`, `created_at`/`updated_at`, with a unique constraint on `(aquarium_id, parameter)`.
- [x] 2.2 Generate the Alembic migration with `task db-migration-new NAME="add_aquarium_parameter_thresholds"` and verify the autogenerated migration matches the model (table, indexes, unique constraint, FK cascade). No migration is needed for temperature measurement support (task group 1) since `aquarium_measurements` columns are already free-text.
- [x] 2.3 Rename the generated migration file and its `revision`/`down_revision` identifiers to the project's date-based convention (`YYYYMMDD_NNNNNN`, e.g. `20260716_000001`) matching preceding migrations in `alembic/versions/`, instead of the default hash-like autogenerate ID.
- [x] 2.4 Run `task db-migrate` locally and confirm the migration applies cleanly and is reversible (`alembic downgrade -1` then `alembic upgrade head`).

## 3. Repository layer (thresholds)

- [x] 3.1 Create `src/aquarium_parameter_threshold_repository.py` with `AquariumParameterThresholdRepository(session)` following the existing repository pattern.
- [x] 3.2 Implement `get_by_aquarium_and_parameter(aquarium_id, parameter) -> AquariumParameterThreshold | None`.
- [x] 3.3 Implement `upsert(aquarium_id, parameter, target, min, max, unit) -> AquariumParameterThreshold` that creates a new row or replaces the existing one for that `(aquarium_id, parameter)` pair (no duplicate rows).
- [x] 3.4 Ensure repository methods do not leak cross-owner data (all lookups are scoped via the aquarium already resolved by `AquariumRepository.get_by_id_and_owner`, matching the measurement router's pattern of checking aquarium ownership before touching threshold data).

## 4. Router & validation (thresholds)

- [x] 4.1 Create `src/aquarium_parameter_thresholds.py` with `build_aquarium_parameter_threshold_router()`, mounted under `/aquariums/{aquarium_id}/thresholds/{parameter}`.
- [x] 4.2 Define `SUPPORTED_THRESHOLD_PARAMETERS = {"temperature", "salinity", "phosphate"}` (now identical to `aquarium_measurements.SUPPORTED_PARAMETERS`); import canonical units and sanity-range constants (`MAX_SALINITY_PPT`, `MIN_SALINITY_SG`, `MAX_SALINITY_SG`, `MAX_PHOSPHATE_PPM`, `MIN_TEMPERATURE_CELSIUS`, `MAX_TEMPERATURE_CELSIUS`) from `aquarium_measurements` rather than redefining them.
- [x] 4.3 Implement `_normalize_parameter` (lowercase + trim + validate against `SUPPORTED_THRESHOLD_PARAMETERS`, mirroring `aquarium_measurements._normalize_parameter`).
- [x] 4.4 Define `SetThresholdRequest` pydantic model with optional `target`, `min`, `max` floats (`extra="forbid"`), and validate `min <= target <= max` when more than one field is present.
- [x] 4.5 Add per-parameter numeric sanity validation using the constants imported in 4.2.
- [x] 4.6 Implement `PUT /aquariums/{aquarium_id}/thresholds/{parameter}`: resolve+check aquarium ownership (404 if not owned/found), normalize parameter, validate payload, upsert via repository, return via `success_response`.
- [x] 4.7 Implement `GET /aquariums/{aquarium_id}/thresholds/{parameter}`: resolve+check aquarium ownership, normalize parameter, return stored thresholds or a successful "not configured" response (nulls) if no row exists.
- [x] 4.8 Mount `build_aquarium_parameter_threshold_router()` in `src/app.py` alongside the other resource routers.

## 5. Tests (thresholds)

- [x] 5.1 Add `tests/test_aquarium_parameter_threshold_repository.py`: create/upsert/replace behavior, uniqueness per `(aquarium_id, parameter)`, cross-owner rejection. (Cascade-on-delete is asserted at the FK/migration level — the SQLite test engine doesn't enforce `PRAGMA foreign_keys`, matching the existing measurement tests' precedent of not testing this at the repository level.)
- [x] 5.2 Add `tests/test_aquarium_parameter_thresholds.py` (router-level) covering: setting thresholds for an owned aquarium (including `temperature`), partial threshold fields, rejecting non-owned aquarium access (404/401), rejecting unsupported parameter, case-insensitive parameter normalization, rejecting inconsistent `min > target > max` ordering, rejecting out-of-range values per parameter (including temperature), retrieving stored thresholds, retrieving an unconfigured parameter (200 + nulls, not 404), and replacing previously stored thresholds on a second `PUT`.
- [x] 5.3 Run `task test` and confirm coverage for the new/changed modules meets the project's 80% target. (92 tests pass; `aquarium_parameter_thresholds.py` 100%, `aquarium_parameter_threshold_repository.py` 90%, `aquarium_measurements.py` 91%, overall 91%.)

## 6. Finalize

- [x] 6.1 Run `task lint`, `task format-check`, and `task typecheck` and fix any issues in the new/changed modules.
- [x] 6.2 Update any relevant OpenAPI/README references if the project documents endpoints outside of the OpenSpec specs (check for an existing API reference doc before adding one). (Updated `docs/api-framework.md`: added `temperature` to the measurement contract's supported parameters and added a new "Parameter Threshold Endpoint Contract" section.)
